import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 這裡可以設定你的網域，例如 site: 'https://newfreeway.mo'
  outDir: './dist',
  publicDir: './public',
});
